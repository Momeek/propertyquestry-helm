apiVersion: v1
kind: Service
metadata:
  name: client-service
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.client.name }}
  ports:
    - protocol: TCP
      port: {{ .Values.client.servicePort }}
      targetPort: client-port
